# Tasks

## 1. Firebase 資料抓取邏輯更新

- [x] 1.1 預設僅查詢使用者自身的標記資料（不使用 `in` 運算子）。
- [x] 1.2 實作延遲載入邏輯：當第一階段篩選器切換至「群組所有」時，才觸發使用 `in` 運算子的查詢，載入群組內所有成員的標記資料。
- [x] 1.3 抓取並映射群組成員的個人資料（名稱），以便在畫面上顯示。

## 2. UI 更新 - 篩選功能

- [x] 2.1 實作第一階段篩選下拉選單（預設為 `自己`，另有選項為 `群組`）。
- [x] 2.2 實作第二階段篩選下拉選單，並設定其在第一階段選為 `群組` 時才顯示。
- [x] 2.3 在第二階段篩選中填入選項：`群組所有` 以及個別成員名稱（例如：`群友 A`、`群友 B`）。
- [x] 2.4 實作雙層篩選狀態邏輯，根據選取的兩階段條件，過濾已抓取到的標記資料並更新畫面。

## 3. 資料彙整邏輯

- [x] 3.1 建立 computed 屬性或方法，將抓取到的標記按賽事（日期）進行彙整。
- [x] 3.2 針對彙整後的資料列，實作邏輯以決定該列的整體標記意向（已購買 > 想去）。
- [x] 3.3 收集所有標記該賽事的成員名稱，整理為陣列或字串以供顯示。

## 4. UI 更新 - 清單欄位與版面

- [x] 4.1 在桌面版檢視中，於「匯出」與「日期」欄位之間新增 `群友` 欄位。
- [x] 4.2 更新清單的渲染邏輯，改為顯示彙整後的標記列，而非個別原始標記。
- [x] 4.3 確保標記圖示反映在步驟 3.2 中計算出的最高權重意向。
- [x] 4.4 將步驟 3.3 中彙整好的成員名稱填入 `群友` 欄位中。
- [x] 4.5 實作響應式行動裝置版面：在小螢幕上將清單檢視重構為堆疊式/卡片式設計，使 `群友` 資訊能夠換行顯示，防止橫向空間擁擠。
