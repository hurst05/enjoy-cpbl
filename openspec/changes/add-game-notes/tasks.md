# Tasks: Add Game Notes

## 1. 備註資料與純函式

- [x] 1.1 在 `_test-scripts-to-delete/` 新增失敗測試，涵蓋備註正規化、網址切分與群友備註彙整
- [x] 1.2 實作 `src/utils/gameNotes.js`，讓純函式測試通過
- [x] 1.3 在 `src/firebase.js` 新增每人每場備註的寫入與清空函式

## 2. 狀態與介面串接

- [x] 2.1 在 `useMarks` 加入儲存備註與成功後更新本地 `userMarks` 的行為
- [x] 2.2 在 `App.vue` 將備註儲存事件串接至 `GameModal`
- [x] 2.3 在 `GameModal.vue` 新增登入者備註輸入、儲存狀態與錯誤處理
- [x] 2.4 在 `GameModal.vue` 新增群友備註清單及安全的 HTTP/HTTPS 連結呈現
- [x] 2.5 補齊桌面與行動版樣式，確保長網址可斷行且控制項易於操作
- [x] 2.6 將已儲存備註與群友備註的顯示區塊移至啦啦隊應援名單下方，保留輸入區在下方
- [x] 2.7 在 `GameCard.vue` 的啦啦隊圖示加入與篩選命中一致的備註提示動畫與減少動態效果支援

## 3. 驗證

- [x] 3.1 執行備註測試腳本並確認所有案例通過
- [x] 3.2 執行 `npm run build` 並修正建置錯誤
- [x] 3.3 在瀏覽器驗證登入、儲存、清空、群友顯示與網址開啟流程
